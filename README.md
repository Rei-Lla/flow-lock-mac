# 心流强制锁定器 - Mac 版本

> 强制锁定指定应用，屏蔽一切干扰，进入深度专注的心流状态。

你是否也有这样的经历：打算专注写代码，却不自觉打开了微信；准备读论文，手指却滑向了浏览器？

**心流强制锁定器**是一款 macOS 桌面应用，它会在你设定的时间内，**强制你只能使用选定的应用**。所有其他应用——无论是从 Dock 点击、Cmd+Tab 切换、还是 Spotlight 打开——都会被瞬间隐藏。直到倒计时结束，你才能重获自由。

没有"再看一眼就好"，没有"就回一条消息"。**物理级别的干扰屏蔽。**

---

## 功能特性

### 应用锁定
- 自动检测当前运行中的所有 GUI 应用
- 勾选你想专注使用的应用（如 VS Code、Terminal、Notes）
- 启动锁定后，**只有被选中的应用可以使用**
- 所有未选中的应用一旦获得焦点，立刻被隐藏并切回锁定应用

### 计时系统
- 预设时长：15 / 30 / 60 / 90 / 120 分钟快速选择
- 支持自定义时长（1~480 分钟）
- 锁定期间显示悬浮倒计时窗口，始终置顶

### 动态增加应用
- 锁定期间可随时点击「增加应用」
- 新增的应用会加入允许列表，**不会重置或延长计时**
- 适用于临时需要打开某个工具的场景

### 紧急解锁
- 遇到紧急情况需要提前解锁？需要手动输入 `我确认要解锁` 完整短语
- 设计上增加了操作成本，防止冲动解锁
- 这不是一个可以随手点掉的按钮

---

## 截图预览

**设置界面** — 选择要锁定的应用和时长

```
┌──────────────────────────────┐
│       心流强制锁定器          │
│   锁定指定应用，进入心流状态   │
│                              │
│  ☑ VS Code                   │
│  ☑ Terminal                  │
│  ☐ Safari                    │
│  ☐ WeChat                    │
│                              │
│  锁定时长  [15] [30] [60]    │
│                              │
│  ┌──────────────────────┐    │
│  │  开始锁定 (2个应用)   │    │
│  └──────────────────────┘    │
└──────────────────────────────┘
```

**锁定中** — 悬浮倒计时栏

```
┌──────────────────────────────────────┐
│  25:30   VS Code + Terminal          │
│  锁定中·2个应用  [增加应用] [解锁]    │
└──────────────────────────────────────┘
```

---

## 安装

### 方式一：下载 DMG（推荐）

前往 [Releases](../../releases) 页面，下载最新的 `.dmg` 文件，拖入 Applications 即可。

### 方式二：从源码构建

```bash
# 克隆仓库
git clone https://github.com/Rei-Lla/flow-lock-mac.git
cd flow-lock-mac

# 安装依赖
npm install

# 开发模式运行
npm run dev

# 构建生产版本
npm run build

# 打包为 DMG
npm run package
```

---

## 权限设置

首次启动时，macOS 会请求 **自动化权限**（Automation），用于检测和管理运行中的应用。

如果应用列表为空，请前往：

> **系统设置 → 隐私与安全性 → 自动化** → 为「心流锁定器」开启 **System Events** 权限

---

## 技术栈

- **Electron** — 跨平台桌面框架
- **React + TypeScript** — 前端界面
- **Vite** — 构建工具
- **AppleScript** — macOS 应用检测与焦点管理

---

## 工作原理

```
启动锁定
    │
    ▼
每 400ms 检查当前最前面的应用
    │
    ├── 在允许列表中 → 放行
    │
    └── 不在允许列表中 → 立即隐藏该应用
                         └── 切换焦点到锁定应用
```

核心机制非常简单：通过 AppleScript 高频检测 macOS 的前台应用进程，一旦发现不在白名单中的应用出现，立即将其设为不可见（`set visible to false`）并激活允许的应用。

这意味着：
- 从 Dock 点击非锁定应用 → 闪一下就消失
- Cmd+Tab 切到非锁定应用 → 瞬间被拉回
- Spotlight 打开新应用 → 立即被隐藏
- **无法绕过，除非使用紧急解锁**

---

## 适用场景

- 需要深度编程但总忍不住看消息
- 写作/阅读时想屏蔽社交软件
- 考试/学习时只允许使用特定工具
- 任何需要"强制自己专注"的时刻

---

## 已知限制

- 仅支持 macOS（依赖 AppleScript 的 System Events）
- 需要授予自动化权限
- 无法阻止通过 Terminal 手动 kill 进程（但如果你都到 Terminal 操作了，说明你是认真的）

---

## 开源协议

[MIT License](./LICENSE)

---

## 贡献

欢迎 PR 和 Issue！如果你有好的想法，比如：

- 锁定期间的专注数据统计
- 每日/每周专注报告
- 自定义主题配色
- 快捷键启动预设方案
- iOS/Android 联动

都欢迎提出讨论。

---

**专注是一种选择。当意志力不够时，让工具来帮你。**
